# ⚠️  TEMPLATE ONLY — Do NOT commit actual secrets to version control.
# In production, use HashiCorp Vault, AWS Secrets Manager, or K8s External Secrets Operator.
#
# To apply in development:
#   kubectl create secret generic boilerplate-secrets \
#     --from-literal=DB_USER=boilerplate \
#     --from-literal=DB_PASS=<your-password> \
#     --from-literal=REDIS_PASSWORD=<your-redis-pass> \
#     --from-literal=JWT_SECRET=<your-256-bit-secret>
#
apiVersion: v1
kind: Secret
metadata:
  name: boilerplate-secrets
type: Opaque
stringData:
  DB_USER: "REPLACE_ME"
  DB_PASS: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_WITH_AT_LEAST_32_CHARACTERS_SECRET_KEY"
